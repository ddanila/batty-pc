# Vendored toolchain — symlinks into sibling repos, not portable.
# See README "Toolchain" for how to set them up on a fresh clone.
vendor/

# Build outputs
build/batty.exe
build/batty.img
build/batty-test.img
build/AUTOEXEC.BAT
build/AUTOEXEC-T.BAT
build/*.obj
build/*.lst

# Re-generatable analysis artifacts (`make regions`, etc.)
# Keep snapshots/ since they're laboriously captured by hand.
build/test_visual/

# Python
__pycache__/
*.pyc

# OS junk
.DS_Store
